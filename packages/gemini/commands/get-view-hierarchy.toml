# these don't do anything, we just need to describe commands in GEMINI.md
prompt = """
This is the current view hierarchy:

Search Results:
!{curl -s -X POST http://localhost:8766/agent -d '{"action":"getViewHierarchy"}'}
"""
